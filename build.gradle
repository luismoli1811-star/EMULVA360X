plugins {
    id 'com.android.application'
}

android {
    namespace "com.luismoli1811star.emulva360x"
    compileSdk 34

    defaultConfig {
        applicationId "com.luismoli1811star.emulva360x"
        minSdk 26
        targetSdk 34
        versionCode 1
        versionName "1.0"
        externalNativeBuild {
            cmake {
                cppFlags "-std=c++20"
                arguments "-DANDROID_STL=c++_shared"
            }
        }
    }

    externalNativeBuild {
        cmake {
            path "../CMakeLists.txt"
        }
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {}
